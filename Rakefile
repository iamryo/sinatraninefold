namespace :db do
  desc "Provide db:setup so Ninefold can run the command"
  task :setup do
    puts " :) "
  end
  
  desc "Provide db:migrate so Ninefold can run the command"
  task :migrate do
    puts " :) "
  end
end

namespace :assets do
  desc "Provide assets:precompile"
  task :precompile do
    puts " :) :) "
  end

  # desc "Pick a random prize and winner"
  # task :all => [:prize, :winner]
end
